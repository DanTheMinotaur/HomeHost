services:
    {{app_name}}:
        container_name: {{app_name}}
        restart: unless-stopped
        volumes:
            - $DOCKER_VOLUMES/rotki/data:/data
            - $DOCKER_VOLUMES/rotki/logs:/logs
        image: rotki/rotki:latest